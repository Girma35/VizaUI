# Tool downloads

Place your software tool files here (e.g. `.zip`, `.exe`, `.dmg`), using the same filenames as in `lib/data.ts` (e.g. `file-sync-pro.zip`).

Or set `downloadUrl` in `lib/data.ts` to external URLs (e.g. GitHub releases, your CDN) for each tool.
